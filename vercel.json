{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "npm install --no-audit --no-fund --legacy-peer-deps --package-lock=false",
  "buildCommand": "npm run build",
  "outputDirectory": "out"
}
